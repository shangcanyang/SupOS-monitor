# 更新说明

## v1.0.1

### 画布规则引擎
- 仅支持新版信号链规则（`model: 'signal'`），移除旧画布规则的兼容代码：
  - 前端删除旧规则面板、旧节点/旧规则判定、旧内容块列表、旧版 JSON 视图等入口；
  - 引擎删除旧版求值分支（tag / compare / trigger 等），遇旧格式规则直接忽略，不再告警；
  - 通知载体节点改为取「执行操作」节点，记录日志类动作不计入触发。
- 连线端口改为按数量动态定位，节点高度随端口数自适应，修复 3 个以上连接点时端口溢出节点的问题（如「任一发生」的三输入）。

### 实时数据
- 移除「粘贴导入」与「规则测试」按钮及其相关逻辑。
- 新增位号批量删除：进入批量模式后卡片显示勾选框，可「删除选中」或「退出批量」，删除前二次确认并显示删除数量。

### 问题修复
- 修复应用启动后 WebSocket 已连接、状态栏仍显示橙色「初始化中」的问题：主进程缓存最近连接状态并新增查询接口，页面加载完成后再补推一次状态。
- 修正批量删除的确认文案（该操作仅删除位号配置，不涉及历史数据）。

### 工程
- 版本号更新至 1.0.1，统一源码行尾并清理冗余代码。

---

## v1.0.0

- 首个发布版本：supOS 测点监控告警 Windows 桌面常驻应用，支持位号管理、上下限告警、邮件推送、画布规则与自动更新。
